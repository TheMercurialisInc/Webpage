/* Global Styles */
body {
    height: 100%;
    color: #fff;
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    overflow-x: hidden;
    background-color: #000;
    background-image: url(../assets/JWSTPIX.png);
    background-attachment: fixed;
    background-repeat: no-repeat;
    background-size: auto 1300px; 
    background-position: top right -200px;
}

.header {
    background-color: transparent;
    padding: 20px;
    text-align: center;
    position: relative; /* Add this line */
}
.header img {
    position: relative; /* Change to relative */
    display: block; /* Ensure proper centering */
    margin: 0 auto; /* Center the image */
    z-index: 1; /* Ensure the logo is above the text */
}
.header h1,
.header h2,
.header h3 {
    position: relative; /* Ensure all text elements are positioned above the image */
    z-index: 2; /* Set a higher z-index for text to appear above the image */
}

.header h1 {
    font-size: 45px;
    color: #ffffff;
    margin-top: -140px; /* Adjust this value according to your layout */
    position: relative; /* Add this line */
    z-index: 2; /* Ensure the text is above the logo */
}

.header h2 {
    font-size: 20px;
    text-align: center;
    color: #fefe5d;
}

.header h3 {
	 font-size: 20px;
    text-align: center;
    color: #576b84;
}

.info-section {
    display: flex;
    background-color: transparent;
    padding: 20px;
    justify-content: center;
}

.margin-column,
.fixed-column,
.main-info-column,
.trim-box,
.link-box,
.navigation {
    background-color: transparent;
}

.margin-column {
    flex-grow: 0;
    width: 200px;
    height: 100%;
    border: 0px solid #ccc;
    padding: 10px;
}

.fixed-column {
    width: 150px;
    max-width: 150px;
    height: auto;
    border: 0px solid #ccc;
    padding: 10px;
    margin-top: -175px;
}

.main-info-column {
    flex-grow: 0;
    width: 600px;
    padding: 20px;
    margin: 0 10px;
    border: 0px solid white;
}

.main-info h3 {
    color: white;
    text-decoration: underline;
    margin-bottom: 20px;
    text-align: center;
    font-size: 30px;
}

.item-title h3 {
    color: white;
    font-weight: bold;
    margin-bottom: 20px;
    font-size: 30px;
    text-align: center;
}

/* Navigation Panel Styling */
.navigation-panel {
    background-color: rgba(0, 0, 0, 0.9); /* Dark, semi-transparent background */
    border: 2px solid gold;
    padding: 15px 20px;
    margin: 20px auto;
    text-align: center;
    max-width: 500px;
    border-radius: 5px;
    box-shadow: 0 0 1spx rgba(255, 215, 0, 0.8); /* Slightly stronger shadow for depth */
}

.navigation-panel ul {
    list-style: none; /* Remove bullet points */
    padding: 0;
    margin: 0;
    display: flex;
    justify-content: center; /* Center links horizontally */
    flex-wrap: wrap; /* Wrap links on smaller screens */
    gap: 20px; /* Larger spacing between links */
}

.navigation-panel ul li {
    margin: 0;
}

.navigation-panel ul li a {
    color: #49ce5a; /* Green text for links */
    text-decoration: none;
    font-size: 18px;
    font-weight: bold;
    padding: 5px 10px; /* Better spacing inside links */
    border: 2px solid transparent; /* Invisible border for hover effect */
    border-radius: 5px; /* Rounded edges */
    transition: all 0.3s ease; /* Smooth transition */
}

.navigation-panel ul li a:hover {
    background-color: #576b84; /* Subtle background color change */
    color: gold; /* Gold text on hover */
    border: 2px solid white; /* Highlight border on hover */
    transform: scale(1.05); /* Slight zoom effect */
}

/* Highlight active page */
.navigation-panel ul li a.active {
    background-color: gold; /* Highlight current page */
    color: black;
    border: 2px solid #49ce5a; /* Green border for active link */
}

/* Responsive Design: Center links on small screens */
@media (max-width: 600px) {
    .navigation-panel ul {
        flex-direction: column; /* Stack links vertically */
        gap: 10px; /* Reduce gap between links */
    }
}

.centerbox1 {
    flex-grow: 0;
    width: 500px;
    border: 3px solid #ffff00;
    padding: 20px;
    margin: auto;
    background-color: #4c0000;
    border: 2px solid #ffff00;
    border-radius: 5px;
    position: inherit;
}

.centerbox2 {
    flex-grow: 0;
    width: 500px;
    border: 3px solid #ffff00;
    padding: 20px;
    margin: auto;
    background-color: #576b84;
    border: 2px solid #ffff00;
    border-radius: 5px;
}

.footer {
    background-color: #4c0000;
    padding: 28px 0;
    text-align: center;
}

.footer p {
    font-size: 12px;
    padding: 0 50px;
    color: #fff;
}

.centeredmedia {
    text-align: center;
}

a:link,
a:visited {
    color: gold;
}

.cyno { 
opacity: 0.8
}

.recruitment {
font-style: normal;
text-align: center;
color: white;
font-size: 20px;

}
.video-section {
    margin: 20px auto;
    padding: 15px;
    width: 80%;
    border: 2px solid #FFD700;
    background: rgba(0, 0, 0, 0.7);
    text-align: center;
}

.video-title {
    color: #FFD700;
    font-weight: bold;
    font-size: 1.5em;
}
